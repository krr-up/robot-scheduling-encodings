% ------------------------------------------------------------------------------
% No choice in moves - but make sure the moves for each path is a sequence
%
% Inputs: task/2, edge/3, robot/1, path/2, move/3.
%
% Outputs: move/3.
%
% ------------------------------------------------------------------------------

% ------------------------------------------------------------------------------
% Every vertex in a move can have at most one incoming and one outgoing
% ------------------------------------------------------------------------------

:- #count{ V' : move(P,V,V') } > 1, move(P,V,_).
:- #count{ V : move(P,V,V') } > 1, move(P,_,V').
:- move(P,_,_), not path(P,_).

% ------------------------------------------------------------------------------
% ------------------------------------------------------------------------------

